{
  "name": "z-monitor-react",
  "version": "1.0.0",
  "description": "z-monitor react lib",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rollup -c",
    "prepublishOnly": "pnpm -F z-monitor-core run build && npm run build",
    "publish":"npm run publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "z-monitor-core": "workspace:^"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "publishConfig": {
    "directory": "dist"
  }
}