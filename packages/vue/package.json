{
  "name": "z-monitor-vue",
  "version": "1.0.0",
  "description": "z-monitor vue lib",
  "main": "dist/index.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rollup -c",
    "prepublishOnly": "pnpm -F core run build && npm run build",
    "publish":"npm run publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "z-monitor-core": "workspace:^"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "publishConfig": {
    "directory": "dist"
  }
}