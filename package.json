{
  "name": "z-monitor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build:vue": "pnpm --filter ./packages/vue run build",
    "build:react": "pnpm --filter ./packages/react run build",
    "build": "pnpm recursive run build",
    "publish": "pnpm -r publish",
    "publish:vue": "pnpm --filter ./packages/vue publish",
    "publish:react": "pnpm --filter ./packages/react publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "zard",
  "license": "ISC",
  "dependencies": {
    "@libc/core": "workspace:^"
  },
  "devDependencies": {
    "@babel/core": "^7.25.8",
    "@babel/preset-env": "^7.25.8",
    "@rollup/plugin-babel": "^6.0.4",
    "rollup": "^4.24.0",
    "rollup-plugin-terser": "^7.0.2",
    "terser": "^5.36.0"
  }
}